# C++54期提高

## Day01

### 一、面向对象设计基本概念

面向对象的分析（OOA）：解析出需求，要做什么。

面向对象的设计（OOD）：需要设计那些类，类中有哪些数据成员、哪些成员函数，类与类之间的关系。

面向对象的编程（OOP）：将需求与设计转换为代码。



UML语言：统一建模语言。

![image-20240402095602239](C++54期提高笔记.assets/image-20240402095602239.png)



### 二、类与类之间的关系（==重要==）

#### 1、继承（泛化）

基类部分会成为派生类的一部分。在语义层面：A  is  B。在类图的画法上：从派生类向基类拉一个空心三角箭头。

![image-20240402101908549](C++54期提高笔记.assets/image-20240402101908549.png)

##### 继承与泛化的区别

继承：先有基类，然后才有派生类。泛化：先有派生类，然后将具有相同属性的抽象成基类。

#### 2、关联

##### 2.1、双向的关联关系

彼此知道对方的存在，但是并不负责对方的生命周期。在语义层面：A has B。在代码层面上，可以使用指针或者引用。在类图的画法：就是一根实心直线。

![image-20240402105914191](C++54期提高笔记.assets/image-20240402105914191.png)

##### 2.1、单向的关联关系

A知道B的存在，但是并不负责对方的生命周期。在语义层面：A has B。在代码层面上，可以使用指针或者引用。在类图的画法：从A指向B的实线箭头。

![image-20240402110337820](C++54期提高笔记.assets/image-20240402110337820.png)

#### 3、聚合

一种强一点的关联关系，有整体与局部的关系，但是整体并不负责局部的生命周期。在语义层面：A has B。在代码层面：使用的是指针或者引用。在类图的画法：可以从局部指向整体的空心菱形箭头。

![image-20240402110938053](C++54期提高笔记.assets/image-20240402110938053.png)

#### 4、组合

是一种更强的关联关系。有整体与局部的关系，但是整体会负责局部的生命周期。在语义层面：A has B。在代码层面：使用的子对象。在类图的画法：可以从局部指向整体的实心菱形箭头。



![image-20240402111719278](C++54期提高笔记.assets/image-20240402111719278.png)

#### 5、依赖

是两个类之间的一种不确定的关系，语义上是一种A use B的关系，这种关系是**偶然的，临时的，并非**
**固定**的。在类图的画法：使用虚线的箭头，从A指向B 。在代码上表现为：

- B作为A的成员函数参数；
- B作为A的成员函数的局部变量（B作为A的成员函数的返回值）；
- A的成员函数调用B的静态方法。

![image-20240402112342771](C++54期提高笔记.assets/image-20240402112342771.png)

#### 总结：

1、从方向来看，继承是垂直关系，其它四种是横向关系。

2、从==耦合程度==来看，依赖 <  关联 <  聚合 <  组合 <  继承

3、从语义来看，继承 is，依赖 use， 关联、聚合、组合 has

4、依赖关系主要在成员函数。其他的四种主要在数据成员的角度。



### 三、面向对象的设计原则（==重要==）

设计的目标：==低耦合、高内聚==。

耦合：强调是类与类之间、模块与模块之间的关系。内聚：强调的是类的内部或者模块内部的关系。

#### 1、单一职责原则

核心思想：一个类，最好只做一件事，只有一个引起它变化的原因

![image-20240402114753777](C++54期提高笔记.assets/image-20240402114753777.png)

![image-20240402114952629](C++54期提高笔记.assets/image-20240402114952629.png)

#### 2、开闭原则（==重要==）

核心思想就是对抽象编程，而不对具体编程，因为抽象相对稳定

![image-20240402143948876](C++54期提高笔记.assets/image-20240402143948876.png)

#### 3、里氏替换原则（==重要==）

核心思想是：派生类必须能够替换其基类。

可以表现为：

- 派生类可以实现基类的抽象方法，体现多态（正好就是C++中多态的含义）
- 派生类可以增加新的个性/功能（C++中的新增功能的特点）
- 派生类不能覆盖基类中的非虚函数（C++中的隐藏）（基类与派生类中对于非虚函数而言，不要同名）

![image-20240402145832942](C++54期提高笔记.assets/image-20240402145832942.png)

![image-20240402145951110](C++54期提高笔记.assets/image-20240402145951110.png)

#### 4、接口分离原则

核心思想：使用多个小的专门的接口，而不要使用一个大的总接口

![image-20240402150700745](C++54期提高笔记.assets/image-20240402150700745.png)

#### 5、依赖倒置原则（==重要==）

核心思想：面向接口编程，依赖于抽象(抽象是稳定的，具体的是在变化的)

![image-20240402151402145](C++54期提高笔记.assets/image-20240402151402145.png)

在大多数情况下，开闭原则、里氏代换原则和依赖倒置原则会同时出现，**开闭原则是目标**，**里氏代换原则是基础**，**依赖倒置原则是手段**  

#### 6、最少知识（知道）原则

减少类与类、模块与模块之间的耦合程度。

![image-20240402152937959](C++54期提高笔记.assets/image-20240402152937959.png)

#### 7、组合复用原则

使用关联、聚合取代继承关系。

![image-20240402160249765](C++54期提高笔记.assets/image-20240402160249765.png)

### 四、设计模式

#### 1、概念

它是==解决特定问题的一系列套路，有一定的普遍性。它是一套被反复使用、多数人知晓的、代码设计经验的总结==。它可以提高代码的可重用性、代码的可读性和代码的可靠性以及可扩展性。  

设计模式有23种。分类创建型、结构型、行为型 。



#### 2、简单工厂

##### 2.1、类图

![image-20240402170253413](C++54期提高笔记.assets/image-20240402170253413.png)

##### 2.2、代码

![image-20240402180327730](C++54期提高笔记.assets/image-20240402180327730.png)

##### 2.3、优缺点

![image-20240402172856374](C++54期提高笔记.assets/image-20240402172856374.png)

#### 3、工厂方法

##### 3.1、类图

![image-20240402180424695](C++54期提高笔记.assets/image-20240402180424695.png)

##### 3.2、代码

![image-20240402180701788](C++54期提高笔记.assets/image-20240402180701788.png)

##### 3.3、优缺点

![image-20240402180842720](C++54期提高笔记.assets/image-20240402180842720.png)

#### 4、抽象工厂

##### 4.1、类图

![image-20240402181339386](C++54期提高笔记.assets/image-20240402181339386.png)

## Day02

### 一、问题回顾

1、类与类之间的关系有哪几种？有什么特点？

2、面向对象的设计原则有哪几种？每种的核心思想是什么？

3、什么是设计模式？有多少种设计模式？分为哪三种类型？



### 二、观察者模式

定义对象的一种**一对多**的依赖关系，当**一个对象的状态发生改变**时，所有依赖于它的对象都得到**通知并被自动更新**  

#### 类图

![image-20240403105032541](C++54期提高笔记.assets/image-20240403105032541.png)

### 三、面向对象的线程封装

#### 1、类图

![image-20240403122705950](C++54期提高笔记.assets/image-20240403122705950.png)

#### 2、几个问题

##### 2.1、将threadFunc函数设置为静态的

![image-20240403123525932](C++54期提高笔记.assets/image-20240403123525932.png)

##### 2.2、run方法的调用

![image-20240403123844866](C++54期提高笔记.assets/image-20240403123844866.png)



## Day03

### 一、基于对象的线程

#### 1、类图

![image-20240405120903386](C++54期提高笔记.assets/image-20240405120903386.png)

#### 2、几个问题

##### 2.1、新增数据成员的设计

![image-20240405121059084](C++54期提高笔记.assets/image-20240405121059084.png)

##### 2.2、回调函数的注册

![image-20240405121135954](C++54期提高笔记.assets/image-20240405121135954.png)

##### 2.3、回调函数的执行

![image-20240405121218461](C++54期提高笔记.assets/image-20240405121218461.png)

##### 2.4、任务的传递

![image-20240405121401254](C++54期提高笔记.assets/image-20240405121401254.png)

### 二、面向对象的PC

#### 1、原理图

![image-20240405110706689](C++54期提高笔记.assets/image-20240405110706689.png)

#### 2、类图

![image-20240405142241510](C++54期提高笔记.assets/image-20240405142241510.png)

#### 3、几个问题

##### 3.1、RAII思想防止死锁

![image-20240405172509103](C++54期提高笔记.assets/image-20240405172509103.png)



![image-20240405172541015](C++54期提高笔记.assets/image-20240405172541015.png)

##### 3.2、虚假唤醒

![image-20240405172638466](C++54期提高笔记.assets/image-20240405172638466.png)

##### 3.3、禁止复制

![image-20240405172802631](C++54期提高笔记.assets/image-20240405172802631.png)

#### 4、代码流程图

![image-20240405173245656](C++54期提高笔记.assets/image-20240405173245656.png)

## Dya04

#### 一、基于对象的PC封装

#### 1、类图

![image-20240406154406449](C++54期提高笔记.assets/image-20240406154406449.png)

#### 2、几个问题

##### 2.1、生产者代码

![image-20240406154504479](C++54期提高笔记.assets/image-20240406154504479.png)

##### 2.2、消费者代码

![image-20240406154543782](C++54期提高笔记.assets/image-20240406154543782.png)

##### 2.3、测试代码

![image-20240406154648797](C++54期提高笔记.assets/image-20240406154648797.png)

### 二、面向对象的线程池封装

#### 1、类图

![image-20240406155548013](C++54期提高笔记.assets/image-20240406155548013.png)

#### 2、几个问题

##### 2.1、线程池中数据成员

![image-20240406155644285](C++54期提高笔记.assets/image-20240406155644285.png)

##### 2.2、线程池中任务执行不完

![image-20240406155800409](C++54期提高笔记.assets/image-20240406155800409.png)

##### 2.3、线程池无法退出原因

![image-20240406155846149](C++54期提高笔记.assets/image-20240406155846149.png)

##### 2.4、解决线程池无法退出的方法

![image-20240406155921900](C++54期提高笔记.assets/image-20240406155921900.png)

但是又会出现新的问题，就是TaskQueue中wait函数在while循环中。

![image-20240406160004813](C++54期提高笔记.assets/image-20240406160004813.png)

![image-20240406160044180](C++54期提高笔记.assets/image-20240406160044180.png)

#### 3、序列图

见UML

### 三、基于对象线程池封装

#### 1、类图

![image-20240406181545949](C++54期提高笔记.assets/image-20240406181545949.png)

#### 2、几个问题

##### 2.1、工作线程获取任务

![image-20240406181716834](C++54期提高笔记.assets/image-20240406181716834.png)

##### 2.2、向线程池添加任务

![image-20240406181923889](C++54期提高笔记.assets/image-20240406181923889.png)

![image-20240406181951416](C++54期提高笔记.assets/image-20240406181951416.png)

##### 2.3、获取任务并且执行

![image-20240406182126092](C++54期提高笔记.assets/image-20240406182126092.png)



## Day05

### 一、C++11的线程

#### 1、对象的创建

```C++
template< class Function, class... Args >
explicit thread( Function&& f, Args&&... args )
```

#### 2、线程的退出

```C++
void join();
```

#### 3、获取线程id

```C++
std::thread::id get_id() const noexcept
```

可以使用线程对象调用成员函数get_id，获取线程的id

但是一般使用的是

```C++
std::thread::id get_id() noexcept;
```

#### 4、线程入口函数的传递方式（==重要==）

##### 4.1、普通函数的形式

![image-20240408100236107](C++54期提高笔记.assets/image-20240408100236107.png)

##### 4.2、函数指针（引用）的形式

![image-20240408100739383](C++54期提高笔记.assets/image-20240408100739383.png)

##### 4.3、函数对象的形式

![image-20240408101409909](C++54期提高笔记.assets/image-20240408101409909.png)

##### 4.4、传递lambda表达式

![image-20240408101724251](C++54期提高笔记.assets/image-20240408101724251.png)

##### 4.5、传递function对象

![image-20240408102221139](C++54期提高笔记.assets/image-20240408102221139.png)

### 二、互斥锁

#### 1、锁mutex

![image-20240408141652010](C++54期提高笔记.assets/image-20240408141652010.png)

#### 2、锁lock_guard

![image-20240408141820791](C++54期提高笔记.assets/image-20240408141820791.png)

#### 3、锁unique_lock

![image-20240408142224457](C++54期提高笔记.assets/image-20240408142224457.png)

### 三、条件变量

#### 1、条件变量condition_variable

![image-20240408142356358](C++54期提高笔记.assets/image-20240408142356358.png)

![image-20240408142532449](C++54期提高笔记.assets/image-20240408142532449.png)

### 四、计算机网络基础

#### 1、协议

通信双方遵循的规则。

#### 2、几层协议

![image-20240408144108543](C++54期提高笔记.assets/image-20240408144108543.png)

#### 3、TCP协议（==重要==）

协议的头能看懂就可以了。

TCP协议是一个传输层的协议、连接的协议、可靠的协议、全双工的协议、字节流的协议；TCP是一个可以进行流量控制的协议 。

##### 三次握手

![image-20240408145330606](C++54期提高笔记.assets/image-20240408145330606.png)

##### 四次挥手

![image-20240408150243109](C++54期提高笔记.assets/image-20240408150243109.png)

#### 4、状态迁移图（==重要==）

11种状态。

![image-20240408151449322](C++54期提高笔记.assets/image-20240408151449322.png)

![image-20240408161852153](C++54期提高笔记.assets/image-20240408161852153.png)

### 五、网络编程

#### 1、基础

在网路环境中要确定一台主机，需要知道ip，在知道主机上的进程需要知道端口号，所以要确定网络环境中的一个进程，可以使用ip + port。

#### 2、字节序

大端：低地址存高位，高地址存低位；小端：低地址存低位，高地址存高位（x86采用小端存储）  

TCP/IP协议规定，==网络数据流应采用大端字节序== 

```C++
#include <arpa/inet.h>
uint32_t htonl(uint32_t hostlong);//h = host n = network l = long s = short
uint16_t htons(uint16_t hostshort);
uint32_t ntohl(uint32_t netlong);
uint16_t ntohs(uint16_t netshort);

in_addr_t inet_addr(const char *cp);

int getsockname(int sockfd, struct sockaddr *addr, socklen_t *addrlen);
int getpeername(int sockfd, struct sockaddr *addr, socklen_t *addrlen);
```

#### 3、两个重要结构体

![image-20240408153315762](C++54期提高笔记.assets/image-20240408153315762.png)

```C++
struct sockaddr
{
    sa_family_t sa_family; /* address family, AF_xxx */
    char sa_data[14]; /* 14 bytes of protocol address */
};

struct sockaddr_in
{
    sa_family_t sin_family; /* address family: AF_INET */
    in_port_t sin_port; /* port in network byte order */
    struct in_addr sin_addr; /* internet address */
};

/* Internet address. */
struct in_addr
{
    uint32_t s_addr; /* address in network byte order */
};
```

#### 4、网络编程原理图

![image-20240408155935900](C++54期提高笔记.assets/image-20240408155935900.png)

#### 5、端口复用

```C++
int opt = 1;
setsockopt(listenfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

int opt = 1;
setsockopt(listenfd, SOL_SOCKET, SO_REUSEPORT, &opt, sizeof(opt));
```



### 六、IO多路复用（==重要==）

#### 1、概念图

![image-20240408162839518](C++54期提高笔记.assets/image-20240408162839518.png)

#### 2、select的使用

##### 2.1、函数接口

```C++
#include <sys/select.h>
#include <sys/time.h>
#include <sys/types.h>
#include <unistd.h>

int select(int nfds, fd_set *readfds, fd_set *writefds, fd_set *exceptfds,
           struct timeval *timeout);
fd_set:位图。
    
void FD_ZERO(fd_set *set);//将set清空为0
void FD_SET(int fd, fd_set *set);//将fd设置到set集合中
void FD_CLR(int fd, fd_set *set);//将fd从set中清除出去
int FD_ISSET(int fd, fd_set *set);//判断fd是否在集合中
```

![image-20240408171716249](C++54期提高笔记.assets/image-20240408171716249.png)

##### 2.2、优缺点

1、文件描述符上限（1024），同时监听的文件描述符1024个，历史原因，不好修改，除非重新编译Linux内核。
2、当监听的文件描述符个数比较稀疏的时候（比如3， 600， 1023），循环判断比较麻烦，所以需要自定义数据结构：数组
3、监听集合与满足监听条件的集合是同一个，需要将原有集合保存  

#### 3、poll的使用

##### 3.1、函数接口

```C++
#include <poll.h>
int poll(struct pollfd *fds, nfds_t nfds, int timeout);
struct pollfd
{
    int fd; /* file descriptor */
    short events; /* requested events */
    short revents; /* returned events */
};

fds:文件描述符数组。
events：POLLIN/POLLOUT/POLLERR
nfds：监控数组中有多少文件描述符需要被监控
```

##### 3.2、优缺点

优点：
1、突破文件描述符1024的上限
2、监听与返回的集合分离
3、搜索范围变小（已经知道是那几个数组）
缺点：
监听1000个文件描述符，但是只有3个满足条件，这样也需要全部遍历，效率依旧低  

#### 4、epoll的使用

##### 4.1、函数接口

```C++
#include <sys/epoll.h>
int epoll_create(int size);

int epoll_ctl(int epfd, int op, int fd, struct epoll_event *event);
epfd：epoll_create函数返回的值
op：EPOLL_CTL_ADD/EPOLL_CTL_MOD/EPOLL_CTL_DEL
    
//等待所监控文件描述符上有事件的产生
int epoll_wait(int epfd, struct epoll_event *events,int maxevents, 
               int timeout);

typedef union epoll_data 
{
    void        *ptr;
    int          fd;
    uint32_t     u32;
    uint64_t     u64;
} epoll_data_t;

struct epoll_event 
{
    uint32_t     events;      /* Epoll events */
    epoll_data_t data;        /* User data variable */
};
```

##### 4.2、优点

- 文件描述符数目没有上限：通过epoll_ctl()来注册一个文件描述符，内核中使用**红黑树**的数据结构来管理所有需要监控的文件描述符。
- 基于事件就绪通知方式：一旦被监听的某个文件描述符就绪，内核会采用类似于**callback的回调机制**，迅速激活这个文件描述符，这样随着文件描述符数量的增加，也不会影响判定就绪的性能。
- 维护就绪队列：当文件描述符就绪，就会被放到内核中的一个**就绪队列**中，这样调用epoll_weit获取就绪文件描述符的时候，只要取队列中的元素即可，操作的时间复杂度恒为O(1)



## Day06

### 一、问题回顾

1、C++11线程库中线程的入口函数的传递方式有哪些？

2、C++11线程库中互斥锁是那个类？lock_guard与unique_lock使用了什么思想？特点是什么？

3、C++11线程库中条件变量与那个锁类型一起使用？

4、OSI有哪七层？TCP协议的特点？什么是三次握手与四次挥手？

5、状态迁移图有哪11种状态？

6、字节序有那两种方式？

7、有哪三种IO多路复用？各有哪些函数？



### 二、五种网络IO模型（==重要==）

#### 1、阻塞式IO

![image-20240409095000741](C++54期提高笔记.assets/image-20240409095000741.png)

#### 2、非阻塞IO

![image-20240409095320803](C++54期提高笔记.assets/image-20240409095320803.png)

#### 3、IO多路复用

![image-20240409095754889](C++54期提高笔记.assets/image-20240409095754889.png)

#### 4、信号驱动IO

![image-20240409100239799](C++54期提高笔记.assets/image-20240409100239799.png)

==总结：在这四种IO模型中，应用进程在第二阶段都是出于阻塞状态的。==

#### 5、异步IO

![image-20240409100610280](C++54期提高笔记.assets/image-20240409100610280.png)

![image-20240409100827823](C++54期提高笔记.assets/image-20240409100827823.png)

### 三、ReactorV1版本

1、Socket类：将所有与套接字相关操作都封装到该类中，包括：套接字的创建、套接字的关闭、套接字的获取。

2、InetAddress类：所有与地址相关的操作都封装到该类中，包括：获取ip、获取端口号、通过ip与port创建本对象，将struct sockaddr_in作为数据成员。

3、Acceptor类：将所有常规函数封装到本类中，包括：地址复用、端口复用、bind函数、listen函数、accept函数。

4、TcpConnection类：如果Acceptor类的对象调用了accept函数的时候，那么就表明三次握手已经建立成功。就可以使用该类的对象进行数据的收发，发送数据send，接受数据receive函数。

5、SocketIO类：将数据的收发操作全部封装到该类中，该类才是真正数据收发的类型。

```C++
recv与read的区别
ssize_t recv(int sockfd, void *buf, size_t len, int flags);
recv与read的区别在与flags参数，如果flags为0的话，那么recv与read是完全等价的。
如果flags = MSG_PEEK，就会将数据从缓冲区读出来，但是不会将数据从缓冲区中移除。
```

两个函数

```C++
#include <sys/socket.h>
//可以通过文件描述符fd获取本端的地址信息，该信息会存放在第二个参数addr中
int getsockname(int sockfd, struct sockaddr *addr, socklen_t *addrlen);

//可以通过文件描述符fd获取对端的地址信息，该信息会存放在第二个参数addr中
int getpeername(int sockfd, struct sockaddr *addr, socklen_t *addrlen);
```

![image-20240409153454008](C++54期提高笔记.assets/image-20240409153454008.png)

### 四、ReactorV2版本

#### 1、伪代码

```C++
void EventLoop::loop()
{
    _isLooping = true;
    while(_isLooping)
    {
        waitEpollFd();
    }
}

void EventLoop::unloop()
{
    _isLooping = false;
}

void EventLoop::waitEpollFd()
{
    nready = epoll_wait();
    if(-1 == nready && errno == EINT)
    {
        
    }
    else if(-1 == nready)
    {
        
    }
    else if(0 == nready)
    {
        
    }
    else
    {
        for(size_t idx = 0; idx < nready; ++idx)
        {
            if(文件描述符 == listenfd)
            {
                //处理新的连接
                handleNewConnection();
            }
            else
            {
                //处理老的连接
                handleMessage();
            }
        }
    }
}

void EventLoop::handleNewConnection()
{
    //三次握手建立成功
    fd = _acceptor.accept();
    //使用文件描述符创建连接con
    TcpConnection con(fd);
    
    //将键值对存放在map
    _conns.insert(fd, con);
}

void EventLoop::handleMessage(int fd)
{
    it = _conns.find(fd);
    if(it != _conns.end())
    {
        //连接是存在的，可以进行数据的收发
    }
    else
    {
        //连接不存在，可以直接让程序退出来
    }
}
```

## Day07

### 一、ReactorV2版本

#### 1、三个半事件

TCP网络编程最本质的是处理三个半事件：

==**连接建立**==：包括服务器端被动接受连接（accept）和客户端主动发起连接（connect）。TCP连接一旦建立，客户端和服务端就是平等的，可以各自收发数据。
==**连接断开**==：包括主动断开（close、shutdown）和被动断开（read()返回0）。
==**消息到达**==：文件描述符可读。这是最为重要的一个事件，对它的处理方式决定了网络编程的风格（阻塞还是非阻塞，如何处理分包，应用层的缓冲如何设计等等）。
消息发送完毕：这算半个。对于低流量的服务，可不必关心这个事件；另外，这里的"发送完毕"是指数据写入操作系统缓冲区（内核缓冲区），将由TCP协议栈负责数据的发送与重传，不代表对方已经接收到数据。

#### 2、类图

![image-20240410142020046](C++54期提高笔记.assets/image-20240410142020046.png)

![image-20240410142200094](C++54期提高笔记.assets/image-20240410142200094.png)

#### 3、添加三个半事件（回调）

Tcp网络编程中虽然有三个半事件，但是我们只关注其中的前三个，即：连接的建立、消息到达、连接断开。三个事件都是与连接相关的，也就是与TcpConnection相关，这三个事件可以使用**回调机制**的形式进行，也就是回调函数的注册与回调函数的执行，但是现在TcpConnection对象的创建在EventLoop中，所以三个事件需要先传递给EventLoop，再交给TcpConnection，而在C++中，回调函数可以使用bind与function结合使用，也就是function可以绑定函数类型，而现在每个事件都与连接相关，所以就是`function<void(const TcpConnectionPtr &)>`,也就是可以将其改名，`using TcpConnectionCallback = function<void(const TcpConnectionPtr &)>`.然后就是EventLoop注册三个回调函数，并且作为桥梁传递给TcpConnection对象，但是EventLoop不会执行该回调函数，因为三个事件的执行只与TcpConnection连接相关，与EventLoop没有关系。最后的结果是，在TcpConnection与EventLoop都注册三个回调函数与设置三个数据成员，但是只有TcpConnection中有三个回调函数的执行。

#### 4、重要代码解析

##### 4.1、数据成员键值对存储

![image-20240410144302851](C++54期提高笔记.assets/image-20240410144302851.png)

![image-20240410142451528](C++54期提高笔记.assets/image-20240410142451528.png)

##### 4.2、三个回调的存储

需要在EventLoop中与TcpConnection分别存储

![image-20240410142546935](C++54期提高笔记.assets/image-20240410142546935.png)

![image-20240410142627005](C++54期提高笔记.assets/image-20240410142627005.png)

##### 4.3、三个事件在EventLoop注册

![image-20240410142709672](C++54期提高笔记.assets/image-20240410142709672.png)

##### 4.4、三个事件在TcpConnection注册与执行

在TcpConnection中注册的时候，需要使用左值，不然后面的连接进来之后，就得不到回调函数的值了。

![image-20240410142759619](C++54期提高笔记.assets/image-20240410142759619.png)

##### 4.5、防止智能指针的误用

![image-20240410142908859](C++54期提高笔记.assets/image-20240410142908859.png)

##### 4.6、EventLoop中epoll_wait参数问题

![image-20240410143113844](C++54期提高笔记.assets/image-20240410143113844.png)

##### 4.7、EventLoop处理新的连接请求

![image-20240410143645106](C++54期提高笔记.assets/image-20240410143645106.png)

![image-20240410143742700](C++54期提高笔记.assets/image-20240410143742700.png)

##### 4.8、EventLoop中老的连接上有数据收发

![image-20240410144053286](C++54期提高笔记.assets/image-20240410144053286.png)

#### 5、流程图

![image-20240411100724208](C++54期提高笔记.assets/image-20240411100724208.png)

## Day08

### 一、ReactorV3版本

#### 1、类图

![image-20240411142741777](C++54期提高笔记.assets/image-20240411142741777.png)

![image-20240411143158121](C++54期提高笔记.assets/image-20240411143158121.png)

#### 2、ReactorV3版本的缺陷

- 如果业务逻辑的处理比较复杂，那么V3版本中的onMessage函数中就不能处理业务逻辑。
- 可以将业务逻辑的处理交给线程池去做。需要将msg交给线程池，但是如果msg处理好之后，也要发送给EventLoop，而发送数据的能力只有TcpConnection，所以就将连接与消息都发送给线程池。
- 就可以将连接TcpConnection的对象con与需要发送的数据msg打包交给MyTask，然后在MyTask的process函数处理msg这个业务逻辑。然后业务逻辑处理好之后通过con进行发送给EventLoop
- 那么线程池在处理好数据之后，就可以立马进行发送给EventLoop，所以需要通知EventLoop接收处理好之后的数据，也就是需要让线程池与EventLoop之间碱性通信。
- 进程或者线程之间进行通信的方式有一种：eventfd



### 二、eventfd的使用

#### 1、作用

主要用于进程或者线程之间进行通信

#### 2、函数接口

```C++
#include <sys/eventfd.h>
int eventfd(unsigned int initval, int flags);
initval:内核计数器的初始值。
flags：可以设置为0
    
返回值：函数返回一个文件描述符，与打开的其他文件一样，可以进行读写操作
```

#### 3、进程间通信的原理图

![image-20240411143702587](C++54期提高笔记.assets/image-20240411143702587.png)

#### 4、eventfd的封装

![image-20240411152351908](C++54期提高笔记.assets/image-20240411152351908.png)

### 三、ReactorV4版本

#### 1、原理图

![image-20240411111334708](C++54期提高笔记.assets/image-20240411111334708.png)

#### 2、类图

![image-20240411173714656](C++54期提高笔记.assets/image-20240411173714656.png)

![image-20240411174052593](C++54期提高笔记.assets/image-20240411174052593.png)

#### 3、流程图

![image-20240411174423925](C++54期提高笔记.assets/image-20240411174423925.png)

## Day09

### 一、ReactorV5版本

#### 1、类图

![image-20240412103610663](C++54期提高笔记.assets/image-20240412103610663.png)

![image-20240412105622403](C++54期提高笔记.assets/image-20240412105622403.png)



总结：

1、read与send是从网络读数据或者将数据发送到网络上的，也就是进行网络的IO操作，将处理网络IO操作的线程称为IO线程。

2、业务逻辑的处理decode、compute、encode的线程称为计算线程，需要有CPU的参数。

3、如果计算花费的时间非常的少的话，此时可以使用基本的reactor模型。

4、如果计算花费的事件非常长的话，也就是计算线程耗时比较严重，那么建议使用Reactor + ThreadPool.

### 二、timerfd的使用

#### 1、作用

这个接口基于文件描述符，通过文件描述符的可读事件进行超时通知，所以能够被用于select/poll/epoll的应用场景

#### 2、函数接口

```C++
#include <sys/timerfd.h>
int timerfd_create(int clockid, int flags);
功能：该函数生成一个定时器对象，返回与之关联的文件描述符。
参数详解：
clockid:可设置为
CLOCK_REALTIME：相对时间，从1970.1.1到目前的时间。更改系统时间 会更改获取的值，它以系统时间为坐标。
CLOCK_MONOTONIC：绝对时间，获取的时间为系统重启到现在的时间，更改系统时间对齐没有影响。
flags: 可设置为
TFD_NONBLOCK（非阻塞），
TFD_CLOEXEC（同O_CLOEXEC）
linux内核2.6.26版本以上都指定为0


int timerfd_settime(int fd, int flags,const struct itimerspec *new_value,
                                   struct itimerspec *old_value);
功能：该函数能够启动和停止定时器
参数详解：
fd: timerfd对应的文件描述符
flags: 
0表示是相对定时器
TFD_TIMER_ABSTIME表示是绝对定时器
new_value:设置超时时间，如果为0则表示停止定时器。
old_value:
一般设为NULL, 不为NULL,
则返回定时器这次设置之前的超时时间

struct timespec 
{
    time_t tv_sec;                /* Seconds */
    long   tv_nsec;               /* Nanoseconds */
};

struct itimerspec 
{
    struct timespec it_interval;  /* Interval for periodic timer */
    struct timespec it_value;     /* Initial expiration */
};
```

#### 3、类图

![image-20240412121044466](C++54期提高笔记.assets/image-20240412121044466.png)